version: 2.1

jobs:
  say_hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Say Hello
          command: echo "Updated config says hi!"

workflows:
  version: 2
  hello-workflow:
    jobs:
      - say_hello
