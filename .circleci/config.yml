version: 2.1

executors:
  docker-executor:
    docker:
      - image: cimg/base:stable
    working_directory: ~/project

jobs:
  build:
    executor: docker-executor
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker image
          command: |
            docker build -t mybillingapp:latest -f backend/Dockerfile .
            docker save mybillingapp:latest > mybillingapp_latest.tar
      - persist_to_workspace:
          root: ~/project
          paths:
            - mybillingapp_latest.tar

  test:
    executor: docker-executor
    steps:
      - checkout
      - attach_workspace:
          at: ~/project
      - setup_remote_docker
      - run:
          name: Load Docker image
          command: docker load -i ~/project/mybillingapp_latest.tar
      - run:
          name: Run tests
          command: echo "Run your Selenium or integration tests here"
  push:
    executor: docker-executor
    steps:
      - checkout
      - attach_workspace:
          at: ~/project
      - setup_remote_docker
      - run:
          name: Load Docker image
          command: docker load -i ~/project/mybillingapp_latest.tar
      - run:
          name: Docker Login
          command: echo $AZURE_PASSWORD | docker login $ACR_NAME.azurecr.io -u $AZURE_USERNAME --password-stdin
      - run:
          name: Docker Push
          command: docker tag mybillingapp:latest $ACR_NAME.azurecr.io/mybillingapp:latest
      - run:
          name: Push Docker Image
          command: docker push $ACR_NAME.azurecr.io/mybillingapp:latest

workflows:
  build_test_push:
    jobs:
      - build
      - test:
          requires:
            - build
      - push:
          requires:
            - test
